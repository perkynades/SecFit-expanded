name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to heroku
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      - uses: actions/setup-ruby@v1
        with:
          ruby-version: '2.6'
      - run: |
          sudo apt-get upgrade -qy
      - run: pip install -r requirements.txt
        working-directory: ./backend/secfit
      - run: |
          sudo apt-get install -y ruby-dev
          gem install dpl
          dpl --provider=heroku --app=new-secfit-frontend --api-key=${{ secrets.HEROKU_STAGING_API_KEY }}
          dpl --provider=heroku --app=new-secfit-backend --api-key=${{ secrets.HEROKU_STAGING_API_KEY }}